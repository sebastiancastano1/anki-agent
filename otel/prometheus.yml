global:
  scrape_interval: 15s

scrape_configs:
  # Métricas derivadas por el Collector (spanmetrics + costo).
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
